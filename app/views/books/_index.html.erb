<table class='table'>
  <tr>
    <th></th>
    <th>Title</th>
    <th>Body</th>
  </tr>
    <% books.each do |book| %>
    <tr>
      <td>
        <%= link_to (book.user) do %>
          <%= image_tag book.user.get_profile_image, size:'50x50' %></td>
        <% end %>
      <td><%= link_to book.title,book_path(book.id) %></td>
      <td><%= book.body %></td>
      <td><%= render 'favorites/favorite', book: book %></td>
    </tr>

    <% end %>
  </table>
